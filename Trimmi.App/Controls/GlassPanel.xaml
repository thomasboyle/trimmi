<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Trimmi_App.Controls.GlassPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border
        x:Name="ShadowHost"
        Padding="1"
        Background="{ThemeResource GlassPanelBorderBrush}"
        CornerRadius="{StaticResource GlassPanelCornerRadius}">

        <Grid>
            <Border
                x:Name="GlassSurface"
                Background="{ThemeResource GlassPanelFillBrush}"
                BorderBrush="#66FFFFFF"
                BorderThickness="1"
                CornerRadius="9" />

            <Border
                x:Name="GrainOverlay"
                CacheMode="BitmapCache"
                CornerRadius="9"
                IsHitTestVisible="False"
                Opacity="0.28">
                <Border.Background>
                    <ImageBrush
                        ImageSource="{StaticResource GrainBitmap}"
                        Stretch="UniformToFill" />
                </Border.Background>
            </Border>

            <ContentPresenter
                x:Name="PanelContentHost"
                Margin="14"
                Content="{x:Bind PanelContent, Mode=OneWay}" />
        </Grid>
    </Border>
</UserControl>
